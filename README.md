# Todo List App

A modern, full-stack Todo List application built with **React**, **TypeScript**, and **Express**.

## Features
- ✨ **Clean UI**: Minimalist dark mode design with smooth animations.
- ⚡ **Fast**: Built with Vite for instant server start.
- 📝 **CRUD**: Create, Read, Update, and Delete tasks.
- 💾 **Persistence**: Tasks are saved locally (server-side JSON).

## Tech Stack
- **Frontend**: React, TypeScript, Tailwind CSS, Framer Motion
- **Backend**: Node.js, Express, TypeScript
- **Storage**: Local JSON file

## Getting Started

1.  **Install Dependencies**
    ```bash
    npm install
    cd client && npm install && cd ..
    ```

2.  **Run Application**
    ```bash
    npm run dev
    ```
    - Server: `http://localhost:3000`
    - Client: `http://localhost:5173`
